-- Extra autostart processes.
-- o.launch_on_start("my-service")

-- Note: syncthingtray registers its own XDG autostart entry
-- (~/.config/autostart/syncthingtray.desktop), so it is not launched here.

-- Pomodoro now lives in the bar as the ecloaiza.pomodoro widget, so the
-- standalone tray applet is no longer started.
